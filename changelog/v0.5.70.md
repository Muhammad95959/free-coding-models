# Changelog v0.5.70 - 2026-08-01

### Changed

- **Website full redesign** — complete overhaul of the landing page and site chrome:
  - Replaced AI slop marketing copy with direct, honest messaging focused on the core value: monitoring 100+ free AI coding endpoints, auto-failover, and SWE-bench ranking
  - Removed all fake "OPERATIONAL" badges, verbose model lists, and placeholder filler content
  - All section headings now use large, bold typography (`text-4xl`→`text-5xl`) with generous whitespace
  - Alternating dark/light-green sections for readability and visual rhythm

- **Color system** — updated to use the brand green `#c3ed06` derived from the FCM logo throughout the entire design system (CSS variables, hero, cards, badges)

- **Tabler Icons** — replaced inline SVGs and Lucide icons with `@tabler/icons-react 3.46.0` across the whole site:
  - Footer links: `IconBrandGithub`, `IconBrandNpm`, `IconBug`, `IconHistory`, `IconUser`, `IconBrandX`, `IconBook2`, `IconDownload`, `IconRocket`
  - Header hamburger: `IconMenu2` / `IconX`
  - Landing page stats: `IconRadar`, `IconBolt`, `IconServer2`
  - Integration cards: `IconTerminal2`, `IconRocket`, `IconBolt`, `IconBrandDocker`
  - Pi Extension checklist: `IconCheck`
  - CTA buttons: `IconRocket`, `IconBrandGithub`

- **Footer redesign** — much bigger text (`text-sm`/`text-base`), `py-16` padding, icon-led link rows with hover accent color, cleaner copyright strip

- **Landing page sections restructured**:
  - `01 — How it works`: 3 real stats (100+ endpoints, <100ms failover, 14 providers) with Tabler Icons
  - `02 — Benchmark ranking`: two-column layout, SWE-bench tier table on dark background
  - `03 — Works everywhere`: 4 integration cards (CLI, OpenCode, OpenClaw & Hermes, Docker) + Pi Extension feature card
  - CTA: "Start in 30 seconds." — minimal, one command, two buttons

- **Surfaces section** — removed "Desktop Tray App" and "Ollama & Local GGUF", replaced "OpenClaw Integration" with "OpenClaw & Hermes", added Pi Extension card

- **Video component** — added `onError` handler to prevent blank spaces when video files are missing
